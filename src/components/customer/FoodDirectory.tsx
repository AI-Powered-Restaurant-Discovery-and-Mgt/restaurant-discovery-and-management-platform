import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export const FoodDirectory = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Food Directory</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Coming soon: Food directory listings</p>
      </CardContent>
    </Card>
  );
};