import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export const Marketplace = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Food Marketplace</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Coming soon: Food marketplace features</p>
      </CardContent>
    </Card>
  );
};