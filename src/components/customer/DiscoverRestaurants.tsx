import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export const DiscoverRestaurants = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Discover Restaurants</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Coming soon: Restaurant discovery features</p>
      </CardContent>
    </Card>
  );
};