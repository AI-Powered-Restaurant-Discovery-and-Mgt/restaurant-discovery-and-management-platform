import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export const Favorites = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Favorites</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Coming soon: Your favorite restaurants and dishes</p>
      </CardContent>
    </Card>
  );
};