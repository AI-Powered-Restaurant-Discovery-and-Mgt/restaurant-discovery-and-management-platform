import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export const Messages = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Messages</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Coming soon: Chat with restaurants and other users</p>
      </CardContent>
    </Card>
  );
};